{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "semanticCommits": "enabled",
  "packageRules": [
    {
      "matchUpdateTypes": ["minor", "patch"],
      "matchCurrentVersion": "!/^0/",
      "automerge": true
    },
    {
      "description": "Everything Renovate touches here is CI and test tooling, never role behaviour. Forcing the chore type keeps dependency bumps out of the changelog and stops them from triggering a Release Please version bump.",
      "matchPackageNames": ["*"],
      "semanticCommitType": "chore"
    }
  ]
}
